FINAL READY-TO-PASTE PROMPT

I am building a production-grade PDF OCR system that must extract both Bengali (primary priority) and English text accurately.

You must redesign my system architecture like a senior ML + backend system design engineer.

Do NOT reuse my old structure. Redesign properly from scratch with clean modular architecture.

The system must be:

Highly accurate (Bangla-first accuracy)

Cost-efficient (minimum Gemini usage)

Smooth and fast like NotebookLM

Scalable

Production-ready

Confidence-driven

Block-level intelligent routing

🎯 CORE REQUIREMENT

PDFs may contain:

Pure Bangla

Pure English

Mixed Bangla + English

Scanned pages

Digitally generated text PDFs

The system must intelligently decide:

When to extract text directly

When to convert PDF page to image

When to use PaddleOCR

When to use Gemini

When no LLM is required

Do NOT blindly convert every PDF to image.

🏗 SMART PDF PROCESSING STRATEGY (MANDATORY)

The system must:

1️⃣ First attempt native text extraction (pdfplumber / PyMuPDF)

IF:

Text exists and Unicode ratio is valid
→ Skip image conversion completely

2️⃣ If page has no extractable text
→ Convert only that page to image

3️⃣ Run PaddleOCR (multilingual model: Bangla + English)

Do NOT convert entire PDF to images by default.
Only convert pages that require OCR.

This is required for smoothness and cost optimization.

⚡ SMOOTHNESS REQUIREMENTS (Like NotebookLM)

To make the system feel smooth:

Use asynchronous background processing

Stream progress updates:

Parsing...

OCR running...

Validating...

Routing...

Process pages in parallel

Process blocks in parallel

Cache OCR results by content hash

Cache Gemini responses by hash

Avoid reprocessing unchanged pages

Use incremental updates

Return partial results early

Never block UI

Perceived smoothness is critical.

Design async architecture with worker queue.

🧠 HYBRID OCR ARCHITECTURE

Pipeline:

Upload PDF
→ Page classification
→ Native text extraction attempt
→ If failed → Image conversion (only that page)
→ PaddleOCR (Bangla + English)
→ Block segmentation
→ Language detection per block
→ Composite confidence scoring
→ Correction layer
→ Selective Gemini fallback (block-level only)
→ Merge structured output

Never send full document to Gemini.

Only low-confidence blocks.

🌍 LANGUAGE-AWARE ROUTING (Bangla Priority)

For each block:

Calculate Bengali Unicode ratio

Calculate English alphabet ratio

Detect language type:

Bangla-heavy

English-heavy

Mixed

Bangla-heavy blocks must use stricter confidence thresholds.

If Bangla confidence is low → higher priority for Gemini.

If English-only and high confidence → never send to Gemini.

📊 COMPOSITE CONFIDENCE SCORING SYSTEM

Design weighted formula using:

OCR engine word confidence

Bangla Unicode ratio

English ratio

Bangla dictionary match %

English dictionary match %

Invalid character ratio

Regex validation

Structural consistency

Cross-block consistency

Define:

High confidence → Accept
Medium confidence → Local correction
Low confidence → Gemini fallback

Bangla-heavy blocks use stricter routing.

🔁 CORRECTION LAYER (Before Gemini)

Before sending to Gemini:

Apply:

Bangla spell correction

English spell correction

Unicode normalization

Matra normalization

OCR error pattern correction

Mixed-language spacing fixes

Recalculate confidence.

Only if still low → Gemini.

🤖 GEMINI FALLBACK RULES

Gemini must:

Receive only low-confidence block

Preserve original Bangla exactly

Not translate anything

Not modify correct English words

Return strict JSON

Be token-optimized

Be cached using content hash

Design minimal, efficient prompt.

🧩 BLOCK-LEVEL ROUTING (MANDATORY)

Never route entire page automatically.

Split into logical blocks (paragraphs or layout blocks).

If one block fails:
→ Send only that block to Gemini
→ Merge corrected output back

This is required for cost optimization.

📁 DIRECTORY STRUCTURE REQUIREMENT

Use clean directory handling:

BASE_DIR
OUTPUT_IMAGES_DIR
JSON_OUTPUT_DIR
MERGED_OUTPUT_DIR

Design system so:

Images saved only for OCR-required pages

JSON saved per page

Final merged JSON saved separately

Include metadata + confidence scores

Ensure safe filename handling and per-run isolation.

🧱 DATA MODEL DESIGN

Each page must contain:

page_id

source_type (native_text / ocr / hybrid)

blocks[]

block_id

detected_language_type

bangla_ratio

english_ratio

raw_text

corrected_text

confidence_score

routing_decision

gemini_used (boolean)

page_confidence_score

page_language_distribution

Document must contain:

overall_confidence

gemini_usage_summary

language_distribution_summary

🚀 PERFORMANCE + SCALABILITY

Design for:

Multi-page PDFs

Large documents

Parallel page processing

Async Gemini calls

Worker queue architecture

Rate limiting

Horizontal scaling

Caching layer

Logging and monitoring

Error traceability

💰 COST OPTIMIZATION STRATEGY

Explicitly define:

When Gemini is never used

Bangla vs English routing difference

Token reduction

Prompt compression

Retry logic

Hash-based caching

Avoid duplicate OCR

Avoid duplicate Gemini calls

System must aggressively minimize API usage.

📦 DELIVERABLES I EXPECT

Provide:

Clean architecture diagram (text-based)

Step-by-step execution pipeline

Routing algorithm pseudocode

Composite confidence formula

Threshold tuning strategy

Language detection logic

Correction layer design

Optimized Gemini fallback prompt

Caching design

Smoothness strategy explanation

Scalability explanation

Why this design is production-grade

Do not give vague advice.

Give implementation-ready system architecture and logic.

Think like a senior ML + distributed systems architect.

Focus on:

Bangla accuracy first

English correctness

Minimum API cost

Smooth UX

Clean modular architecture

Real-world feasibility